"use strict"

//Deshabilita Rotación Automática Durante la Interacción

/*modelViewer.addEventListener('mousedown', () => {
    modelViewer.autoRotate = false; // Pausar auto-rotación
});

modelViewer.addEventListener('mouseup', () => {
    modelViewer.autoRotate = true; // Reanudar auto-rotación
});
*/
